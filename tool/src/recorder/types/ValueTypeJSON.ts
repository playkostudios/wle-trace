export enum ValueTypeJSON {
    // Void type doesn't exist; just don't include a return type in the type map
    Uint32 = 'u32',
    Int32 = 'i32',
    Int53 = 'i53',
    Float32 = 'f32',
    Float64 = 'f64',
    Boolean = 'bool',
}